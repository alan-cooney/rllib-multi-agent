[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools"]

[project]
dependencies = [
  "atari_py",
  "gym[accept-rom-license]",
  "gym[atari]",
  "pettingzoo",
  "pettingzoo[all]",
  "pygame",
  "ray[rllib]",
  "supersuit",
  "tensorflow",
  "torch"
]
name = "rllib-multi-agent"
version = "0.0.1"
